import { Testimonial } from "@/types";

/**
 * Testimonial registry.
 * Strictly adhering to zero fabricated credibility:
 * Hidden until real, authorized client quotes are provided.
 */
export const testimonialsData: Testimonial[] = [];

export const showTestimonialsSection = false;
